# 🧩 Система компонентов DocLogic

Гибкая модульная система компонентов для легкой верстки и кастомизации лендинга.

## 📦 Компоненты

### `<Section>` — Секция страницы

Обертка для секций с настраиваемыми отступами.

**Пропсы:**
- `id?: string` — ID для якорных ссылок
- `class?: string` — Дополнительные CSS классы
- `padding?: 'none' | 'small' | 'default' | 'large'` — Размер отступов (по умолчанию: `default`)

**Пример:**
```astro
<Section id="benefits" padding="default">
  <!-- контент -->
</Section>
```

---

### `<Container>` — Контейнер контента

Центрированный контейнер с настраиваемой шириной.

**Пропсы:**
- `class?: string` — Дополнительные CSS классы
- `size?: 'sm' | 'md' | 'lg' | 'xl'` — Размер контейнера (по умолчанию: `lg`)

**Пример:**
```astro
<Container size="lg">
  <!-- контент -->
</Container>
```

---

### `<FlexGrid>` — Гибкая сетка

Компонент для создания адаптивных сеток (grid или flex).

**Пропсы:**
- `cols?: 1 | 2 | 3 | 4` — Количество колонок (по умолчанию: `2`)
- `gap?: 'sm' | 'md' | 'lg'` — Размер отступов между элементами (по умолчанию: `md`)
- `class?: string` — Дополнительные CSS классы
- `type?: 'grid' | 'flex'` — Тип layout (по умолчанию: `grid`)
- `items?: 'start' | 'center' | 'end' | 'stretch'` — Выравнивание элементов (по умолчанию: `stretch`)

**Примеры:**

```astro
<!-- Сетка из 3 колонок -->
<FlexGrid cols={3} gap="md">
  <Card>Элемент 1</Card>
  <Card>Элемент 2</Card>
  <Card>Элемент 3</Card>
</FlexGrid>

<!-- Flex layout -->
<FlexGrid type="flex" gap="lg">
  <div>Элемент 1</div>
  <div>Элемент 2</div>
</FlexGrid>

<!-- Сетка с выравниванием по центру -->
<FlexGrid cols={2} items="center">
  <Card>Элемент 1</Card>
  <Card>Элемент 2</Card>
</FlexGrid>
```

**Адаптивность:**
- `cols={1}` → всегда 1 колонка
- `cols={2}` → 1 колонка на мобильных, 2 на планшетах и выше
- `cols={3}` → 1 на мобильных, 2 на планшетах, 3 на десктопах
- `cols={4}` → 1 на мобильных, 2 на планшетах, 4 на десктопах

---

### `<Card>` — Карточка

Универсальный компонент карточки с разными вариантами стилей.

**Пропсы:**
- `class?: string` — Дополнительные CSS классы
- `padding?: 'sm' | 'md' | 'lg'` — Размер внутренних отступов (по умолчанию: `md`)
- `variant?: 'default' | 'feature' | 'highlight' | 'dark'` — Вариант стиля (по умолчанию: `default`)

**Варианты:**
- `default` — Стандартная белая карточка с тенью
- `feature` — Карточка с flex-col для вертикального контента
- `highlight` — Карточка с светло-серым фоном
- `dark` — Темная карточка (для CTA секций)

**Примеры:**

```astro
<!-- Стандартная карточка -->
<Card>
  <h3>Заголовок</h3>
  <p>Контент</p>
</Card>

<!-- Карточка-фича -->
<Card variant="feature" padding="lg">
  <div class="badge">Новое</div>
  <h3>Заголовок</h3>
  <p>Описание</p>
</Card>

<!-- Темная карточка для CTA -->
<Card variant="dark" padding="lg">
  <h2>Запросить предложение</h2>
  <p>Описание</p>
</Card>
```

---

## 🎨 CSS Переменные

Все основные значения можно кастомизировать через CSS переменные в `global.css`:

```css
:root {
  --section-padding-y: 2.5rem;      /* py-10 */
  --section-padding-y-md: 3.5rem;   /* md:py-14 */
  --gap-sm: 0.75rem;                 /* gap-3 */
  --gap-md: 1.25rem;                 /* gap-5 */
  --gap-lg: 2rem;                    /* gap-8 */
  --container-max-width: 72rem;      /* max-w-6xl */
  --primary-bg: rgb(15 23 42);        /* slate-900 */
  --card-bg: white;
  --text-primary: rgb(15 23 42);
  --text-secondary: rgb(71 85 105);
  --text-muted: rgb(100 116 139);
}
```

## 📝 Примеры использования

### Простая секция с карточками

```astro
---
import Section from '../components/Section.astro';
import Container from '../components/Container.astro';
import FlexGrid from '../components/FlexGrid.astro';
import Card from '../components/Card.astro';
---

<Section id="features">
  <Container>
    <h2 class="h2">Возможности</h2>
    <p class="p mt-2">Описание секции</p>

    <FlexGrid cols={3} gap="md" class="mt-8">
      <Card variant="feature">
        <div class="text-base font-semibold">Фича 1</div>
        <p class="p mt-2 text-sm">Описание</p>
      </Card>
      <Card variant="feature">
        <div class="text-base font-semibold">Фича 2</div>
        <p class="p mt-2 text-sm">Описание</p>
      </Card>
      <Card variant="feature">
        <div class="text-base font-semibold">Фича 3</div>
        <p class="p mt-2 text-sm">Описание</p>
      </Card>
    </FlexGrid>
  </Container>
</Section>
```

### Секция с двумя колонками

```astro
<Section id="about">
  <Container>
    <FlexGrid cols={2} gap="lg">
      <div>
        <h2 class="h2">О нас</h2>
        <p class="p mt-2">Текст</p>
      </div>
      <Card>
        <h3>Преимущества</h3>
        <ul>
          <li>Пункт 1</li>
          <li>Пункт 2</li>
        </ul>
      </Card>
    </FlexGrid>
  </Container>
</Section>
```

### CTA секция с темной карточкой

```astro
<Section id="cta">
  <Container>
    <Card variant="dark" padding="lg">
      <FlexGrid cols={2} gap="lg">
        <div>
          <h2 class="text-3xl font-bold">Запросить предложение</h2>
          <p class="mt-3 text-white/80">Описание</p>
        </div>
        <form>
          <!-- форма -->
        </form>
      </FlexGrid>
    </Card>
  </Container>
</Section>
```

## 🔧 Кастомизация

### Изменить количество колонок

Просто измените `cols` в `<FlexGrid>`:
```astro
<!-- Было 3 колонки -->
<FlexGrid cols={3}>

<!-- Стало 2 колонки -->
<FlexGrid cols={2}>
```

### Изменить отступы между элементами

Измените `gap`:
```astro
<!-- Маленькие отступы -->
<FlexGrid cols={3} gap="sm">

<!-- Большие отступы -->
<FlexGrid cols={3} gap="lg">
```

### Изменить отступы секции

Измените `padding` в `<Section>`:
```astro
<!-- Без отступов -->
<Section padding="none">

<!-- Большие отступы -->
<Section padding="large">
```

### Переопределить стили через классы

Используйте `class` проп для добавления кастомных стилей:
```astro
<FlexGrid cols={2} class="custom-grid">
  <!-- контент -->
</FlexGrid>
```

## ✅ Преимущества системы

1. **Легко менять layout** — просто меняете `cols` и `type`
2. **Адаптивность из коробки** — все компоненты адаптивные
3. **Переиспользуемость** — один компонент для всех секций
4. **Кастомизация** — CSS переменные для быстрой настройки
5. **Чистый код** — меньше дублирования, больше структуры

## 🚀 Быстрый старт

1. Импортируйте нужные компоненты:
```astro
---
import Section from '../components/Section.astro';
import Container from '../components/Container.astro';
import FlexGrid from '../components/FlexGrid.astro';
import Card from '../components/Card.astro';
---
```

2. Используйте в разметке:
```astro
<Section id="my-section">
  <Container>
    <FlexGrid cols={3}>
      <Card>Контент 1</Card>
      <Card>Контент 2</Card>
      <Card>Контент 3</Card>
    </FlexGrid>
  </Container>
</Section>
```

Готово! 🎉

