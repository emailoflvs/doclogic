# Environment variables
.env
.env.local
.env.*.local
api/.env
api/.env_*
api/.env.b*

# Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
# package-lock.json не игнорируем - нужен для Docker build

# Build outputs
dist/
build/
.webpack/
.astro/

    Из накладной — в данные
    Фото/файл документа → AI-анализ → подтверждение → CRM / 1C / Excel / бух. учёта и любое другое
    Изображение

    блок 1
    Оператор фотографирует накладную в Telegram или загружает файл через сайт
    2
    DocLogic извлекает абсолютно все данные из документа
    3
    Вы подтверждаете данные отправляются в CRM / 1C / Excel / учёт и пр.


# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Logs
logs/
*.log

# Docker
*.pid
*.seed
*.pid.lock
docker-compose.yml.bak

# Python
__pycache__/
*.py[cod]
*.pyo
.pytest_cache/
.coverage
htmlcov/
docker-compose.yml.*.backup

# Temporary files
tmp/
temp/
.cache/

